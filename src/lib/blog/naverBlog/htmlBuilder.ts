import type { Place } from "@/types/place";

export function buildSmartEditorHtml(_args: {
  reviewHtml: string;
  photoUrls: string[];
  place?: Place;
}): string {
  throw new Error("Phase 5-A: build SmartEditor-compatible HTML");
}
